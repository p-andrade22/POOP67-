import subprocess
import time
import sys
proceso_actual =None 
def detener_ejecucion():
	global proceso_actual
	if proceso_actual.po1l( is None:
		print(f" Intentando detener el procesoanteior(PID:
								
